ADD_LIBRARY(libk STATIC)
TARGET_SOURCES(libk PRIVATE stdlib.c stdio.c string.c)
TARGET_COMPILE_DEFINITIONS(libk PUBLIC __is_libk)
INCLUDE_DIRECTORIES(include)
SET_TARGET_PROPERTIES(libk PROPERTIES PREFIX "")
